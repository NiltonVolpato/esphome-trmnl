# Factory variant — adds pre-adoption / factory-flash extras.
# Includes project metadata, dashboard import URL, improv_serial, and factory_reset.
#
# Requires the ${device_type} substitution to be set (provided by cloud-firmware.yaml).

packages:
  factory_extras: !include ../../devices/${device_type}/factory-extras.yaml
